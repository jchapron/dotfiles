# Global preferences (Jérôme)

- Primary language TypeScript (strict). Also Python (uv, ruff), SQL, Terraform.
- Package managers: pnpm for JS/TS unless the repo says otherwise; uv for Python.
- Keep changes minimal and focused; no drive-by refactors. Match the repo's existing style.
- Run the repo's own checks before declaring done: typecheck, lint, tests.
- Commit messages: conventional commits, imperative, under 72 chars on the first line.
- Prefer small PRs. Use `gh` for GitHub. Never force-push shared branches.
- Environment: macOS, zsh, Ghostty, tmux, Neovim. Editor for interactive edits is `nvim`.
